import 'package:flutter/material.dart';

class ReservedTab extends StatelessWidget {
  const ReservedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Reserved')));
  }
}
