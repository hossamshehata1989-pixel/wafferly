import 'package:flutter/material.dart';

class ExpenseToastLayer extends StatelessWidget {
  const ExpenseToastLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
